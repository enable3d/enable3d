## Installation

Windows:

```console
.\prune.ps1; npm i --legacy-peer-deps; npm run build
```

Linux:

```console
npm run prune && npm i --legacy-peer-deps && npm run build
```

## Start

```console
npm start
```
